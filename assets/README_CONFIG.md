# Here lies a missing file

Because security reasons.

There should be a file named `config.js`, with `module.exports` having these keys:

* APIKEY (for Twitter - the long one combining key and secret)
* storage
* dbhost
